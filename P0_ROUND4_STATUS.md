# WebArena P0 第四轮冻结未见任务 2×2 验收状态

**完成时间**：2026-09-12 04:32（CST）
**冻结清单**：`configs/webarena_p0_round4_holdout_frozen.json`（freeze SHA-256 `6082307e650afa7a4249d29053ad28b508bcbd3eb294357f123d814397f2ed5e`）
**证据**：`data/reports/webarena_p0_round4_2x2_analysis.json`、8 份单元格报告、逐条轨迹

---

## 一、设计

| 项目 | 设置 |
|---|---|
| 站点 | GitLab、Shopping（Reddit 本地可判分任务池已耗尽，排除） |
| 任务 | 每站 40 个冻结未见任务 |
| seed | 0 / 1 / 2 |
| 单元格 | Reactive / W4 × 导航护栏 on / off |
| 步数预算 | 两种策略均为 12 步 |
| 规模 | 40 × 3 × 2 × 2 × 2 = 960 episodes（本次新增 world-model 480 条） |
| 评测器异常 | 0 条（8 个单元格 failures 全为空） |

---

## 二、结果

| 单元格 | 成功/总数 | 成功率 | Wilson 95% CI |
|---|---:|---:|---|
| GitLab · Reactive · 护栏关 | 0/120 | 0.0% | [0.0%, 3.1%] |
| GitLab · Reactive · 护栏开 | 2/120 | 1.7% | [0.5%, 5.9%] |
| GitLab · W4 · 护栏关 | 0/120 | 0.0% | [0.0%, 3.1%] |
| GitLab · W4 · 护栏开 | 2/120 | 1.7% | [0.5%, 5.9%] |
| Shopping · Reactive · 护栏关 | 9/120 | 7.5% | [4.0%, 13.6%] |
| Shopping · Reactive · 护栏开 | 9/120 | 7.5% | [4.0%, 13.6%] |
| Shopping · W4 · 护栏关 | 9/120 | 7.5% | [4.0%, 13.6%] |
| Shopping · W4 · 护栏开 | 9/120 | 7.5% | [4.0%, 13.6%] |

**配对分析（同任务、同 seed 配对，bootstrap 20,000 次）**

| 对比 | 配对单元 | 平均提升（百分点） | 95% CI |
|---|---:|---:|---|
| W4 − Reactive · 护栏关 | 240 | 0.00 | [0.00, 0.00] |
| W4 − Reactive · 护栏开 | 240 | 0.00 | [0.00, 0.00] |
| 护栏效应（Reactive） | 240 | +0.83 | [0.00, +0.83] |
| 护栏效应（W4） | 240 | +0.83 | [0.00, +0.83] |

---

## 三、结论

1. **第四轮未达验收目标**。申报书要求"相对反应式基线任务成功率相对提升 10% 以上"，本轮世界模型主效应为 **0.00 个百分点**，配对置信区间退化为 [0, 0]。
2. **这是第三轮连续零主效应**：Round-2 为 0.00pp，Round-3 点估计 +33.3%（仅多 1 个成功、CI 下界 0、跨护栏主效应 0），Round-4 为严格 0.00pp。
3. **配对差异严格为零的含义**：在 240 个配对单元上，两个策略的成功/失败向量完全相同——世界模型重排改变了执行动作，但没有改变任何一个 episode 的成败。
4. **动作序列确实不同，但不能直接归因于世界模型**。跨策略动作序列在多数配对中不同（GitLab 护栏关 120/120 不同、Shopping 两格 120/120 不同），然而**同一策略的重复运行也会不同**：GitLab Reactive 护栏开 193 次重复中 116 次一致；W4 护栏关 481 次重复中 226 次一致。环境与执行时序本身存在随机性，因此序列差异不能作为"世界模型改变了决策"的证据，除非做受控重跑。**真正干净的证据是成败向量完全相同。**
5. **根因判断**：成败由世界模型无法触及的上游能力决定——候选动作生成、站点流程进入、输入与提交。失败归因（`WEBARENA_FAILURE_ATTRIBUTION.md`）显示 1286 条失败中 30% 是循环/停滞、22% 是从未进入目标流程页面、20% 是预算耗尽；真值率过低使任何下游重排都无区分度。
6. **护栏效应**：+0.83pp（CI 上界 +0.83，下界 0），同样不能声称有效。

---

## 四、修复记录：分析脚本的 pooled 统计缺陷

`scripts/analyze_webarena_p0_round4_2x2.py` 的 pooled 段原先把"四格都判过的任务集合"跨站点求交集，而 GitLab 与 Shopping 的冻结任务池互不相交，交集恒为空，脚本在第一轮执行时直接抛 `StatisticsError`。已修正为**先在各站点内部求交集、再跨站点汇总配对单元**；`paired_pooled` 现为 240 个配对单元。该缺陷不影响每站结果（`paired_per_site` 原本就是按站点分别计算的）。

---

## 五、后续动作

1. 停止把"接入世界模型"当作提升手段：动力学精度提升（H2/H3 MSE 优于 persistence）与决策质量无因果关系，本轮再次证实。
2. 资源转向 W1 基线策略：按失败归因的模块工作量排序推进（循环恢复 → 站点流程知识 → 候选优先级 → 候选生成）。
3. 下一轮评测的门禁：**冻结 holdout 的 reactive 基线必须 ≥ 12%**，否则 10% 相对提升小于配对噪声，结论不可用。
4. 保留本轮 480 条 world-model 轨迹作为反馈回灌语料，用于 W2 的决策后悔驱动重训。
