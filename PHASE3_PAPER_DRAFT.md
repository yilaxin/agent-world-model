# 置信度感知的世界模型浏览器 Agent 候选动作前瞻决策

## 摘要

反应式浏览器 Agent 通常只根据当前页面生成下一动作，容易在长任务中出现重复操作、目标偏离和错误累积。本文实现一种动作条件世界模型驱动的有限步前瞻决策方法。系统首先从可访问性树生成结构合法的候选动作，然后利用一步潜在动力学模型递归推演 1～3 步状态轨迹，并联合短期进展、长期结果、预测不确定性和任务—元素结构一致性对首动作进行重排序。规划深度和世界模型影响权重由置信度动态控制，低置信度时触发重观测或反应式回退。RTX 4090 上的 231 条独立 MiniWoB 测试转移结果表明，系统实现了 100% 候选合法率和解释字段完整率；对已观测成功动作的 Top-1 一致率为 86.36%，高于反应式基线的 72.73% 和一步世界模型的 63.64%。本文同时明确指出，当前结果是离线代理评测而非 WebArena 在线成功率。

关键词：世界模型；浏览器 Agent；候选动作；有限步规划；结构一致性；不确定性

## 1. 问题定义

在时刻 `t`，Agent 接收结构化页面状态 `s_t` 与任务目标 `g`，生成候选集合：

`A_t={a_t^1,…,a_t^K}`。

阶段二模型 W0 学习动作条件转移：

`p(z_{t+1}, Δs_t, y_t, r_t | z_t, a_t)`，

其中 `z_t` 为结构化状态向量，`Δs_t` 表示页面状态变化，`y_t` 包括终止与无效动作信号，`r_t` 包括成功、停滞、目标偏离和严重失败等结果概率。本阶段目标不是继续训练 W0，而是把 W0 组合成可以比较候选动作的前瞻规划器。

## 2. 方法

### 2.1 候选动作生成

确定性生成器从 AXTree 提取可见交互元素，按照角色构造 `click`、`fill`、`select_option` 等动作，并补充有限的滚动与返回动作。所有动作在进入世界模型前进行语法、动作类型、目标元素可见性和去重检查。系统还提供严格 JSON 的 LLM/VLM 适配器，但实验不依赖外部 API。

### 2.2 有限步潜在推演

对每个首动作递归调用 W0，将预测的下一状态向量作为下一步输入，并传递 GRU 隐状态。规划步长限制为 1～3。后续候选动作来自当前候选集，按首动作分组执行 beam search。终止概率作为存活权重，已经预测终止的轨迹不会继续累积同等强度的短期收益或不确定性。真实执行一个动作后，Agent 重新观察页面并生成新候选集，从而避免把潜在状态误当作可执行 DOM。

### 2.3 综合评分

候选首动作采用以下评分：

`J(a)=w_c[J_short(a)+λJ_long(a)-κU(a)]+ηS_struct(a)`。

`J_short` 聚合预测进度、奖励、目标元素可用性，并惩罚无效动作和停滞；`J_long` 综合成功、终止、停滞、目标偏离和严重失败概率；`U` 是异方差头给出的平均不确定性；`S_struct` 根据目标元素可见性、动作—角色兼容性以及任务词与元素名称的语义重叠计算。`w_c` 由置信度控制，防止不可靠世界模型支配决策。

### 2.4 置信度自适应

先执行一步预评估，使用不确定性、结构一致性和 Top-2 分差计算置信度。高置信度使用 H=3 和完整模型权重；中置信度缩短为 H=1 并降低模型影响；低置信度要求重观测；极低置信度回退到反应式策略。该机制实现了申报书提出的“根据置信度动态调整预测结果在决策中的影响”。

## 3. 实验设置

阶段二正式数据包含 1536 条转移、713 个 episode、32 个任务，按 episode 划分为 1089/216/231 条训练、验证和测试样本。W0 在 RTX 4090、PyTorch 2.1.2+cu121 环境训练，最佳 epoch 为 34。阶段三只使用 231 条测试样本及其原始 AXTree 做评估，不参与参数训练。

比较方法包括：反应式规则策略、一步 W0 重排序、本文 H=1～3 前瞻规划。由于轨迹数据只记录实际执行动作的结果，评价采用三类代理指标：候选动作覆盖与合法性、已观测成功动作一致率、已观测风险动作分流率。替代候选没有真实反事实标签。

## 4. 结果

系统在 231 条测试转移上实现原始状态匹配率、候选合法率、解释完整率和有限步合规率均为 100%。安全记录动作 Recall@K 为 100%，平均候选数 4.20。191 个状态采用 H=3，40 个状态采用 H=1；平均置信度 0.8469。RTX 4090 平均推理时延为 39.31 ms，P95 为 91.37 ms。

已观测成功动作 Top-1 一致率方面，反应式基线为 72.73%，一步 W0 为 63.64%，阶段三为 86.36%。已观测风险动作分流率分别为 40.94%、73.23% 和 72.44%。结果说明，结构一致性有效修正了一步 W0 对不同元素 ID 的不稳定偏好，同时保留大部分风险抑制能力。

## 5. 局限性

首先，实验尚未报告 WebArena 在线任务成功率，因此不能据此宣称项目总体成功率已经提高 10%。其次，严重失败标签在测试集没有正样本，对应风险头未得到完整验证。再次，当前确定性候选生成器适合可访问性树明确的页面，复杂视觉目标仍需要绑定 VLM。最后，多步推演复用当前候选集合，无法在潜在状态中构造新的 DOM；真实系统通过每步执行后重观测缓解这一问题。

## 6. 结论与下一步

本文完成了从一步动作条件世界模型到完整前瞻决策 Agent 的系统集成，验证了候选生成、有限步想象、综合评分、结构约束和置信度回退可以在统一接口中稳定运行。下一步将进入阶段四：在真实环境执行后计算预测—现实偏差和决策遗憾，形成反馈驱动的动态重规划闭环；同时补充 WebArena 在线评测、严重失败正样本和 AndroidWorld 迁移实验。
